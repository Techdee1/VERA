# Lua platform key (optional if already configured via lua auth)
LUA_API_KEY=

# Required if using live reasoning mode
OPENAI_API_KEY=

# Optional fallback provider key
GROQ_API_KEY=

# Optional remote MCP integration (Task 6 scaffold)
AML_INTEL_MCP_URL=
AML_INTEL_MCP_TOKEN=

# Optional key guard for transaction-intake webhook (must match backend LUA_TRANSACTION_INTAKE_KEY)
INTAKE_WEBHOOK_KEY=
